var addDigits = function(num) {
    while (num >= 10){
        let s = 0;
        while (num > 0) {
            s += num % 10
            num = Math.floor(num/10);
        }
        num = s
    }
    return num;
};
